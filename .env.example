# Turso Database
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=

# Unbound Gateway API Key (recommended — routes all AI calls through gateway)
# If set, overrides individual provider keys below
UNBOUND_API_KEY=

# Individual AI Provider API Keys (fallback if UNBOUND_API_KEY not set)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=

# Cron Secret (protects cron endpoints)
CRON_SECRET=

# App URL (for share links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
